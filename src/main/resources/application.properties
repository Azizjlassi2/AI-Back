spring.application.name=backend

server.port=${SERVER_PORT:8080}

# Database configuration
spring.datasource.driver-class-name=${DRIVER_CLASS_NAME}
spring.datasource.url=jdbc:postgresql://aws-0-eu-west-3.pooler.supabase.com:5432/postgres?user=postgres.ehwagvxqimizsozojwrm&password=mohamed@aziz1
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.jpa.properties.hibernate.globally_quoted_identifiers=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}


frontend.url=${FRONTEND_URL}


# Logging configuration
debug=true
logging.level.org.springframework.web.cors=debug
logging.level.org.springframework.security.authentication=debug
logging.level.org.springframework.security.web=debug



# Mail Configuration
spring.mail.host=${EMAIL_HOST}
spring.mail.port=${EMAIL_PORT}
spring.mail.username=${EMAIL_USERNAME}
spring.mail.password=${EMAIL_PASSWORD}
spring.mail.properties.mail.smtp.starttls.enable=${TLS_ENABLED}





